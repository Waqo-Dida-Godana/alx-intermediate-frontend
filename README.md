# 0x04-TypeScript

ALX Frontend — TypeScript Project  
Includes tasks 0–11 covering interfaces, classes, namespaces, declaration merging, nominal typing, generics, ambient declarations, DOM manipulation, and webpack.
